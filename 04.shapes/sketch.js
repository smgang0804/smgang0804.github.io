
function setup() {
  createCanvas(600, 400);
  background(200);

  // push(); // Start a new drawing state
  // strokeWeight(5);
  // stroke(255,0,0);
  // point(150, 120);
  // //noStroke(); -> 뒤에 도형은 다 지워짐
  // pop(); // Restore original state
  // point(150, 180);

  // push(); // Start a new drawing state
  // strokeWeight(10);
  // stroke(0,255,0);
  // line(0, 0, 300, 300);
  // pop();

  // line(300, 0, 0, 300);

  // push();
  // //rectMode => CORNER, CORNERS, CENTER, or RADIUS
  // rectMode(CENTER)  
  
  // push();
  // strokeWeight(3);
  // stroke(0,255,255);
  // fill(255,0,0);
  // rect(150,150,100,100);
  // pop();

  // pop();

  //   //ellipseMode => CENTER, RADIUS, CORNER, or CORNERS
  // ellipseMode(CENTER)
  // ellipse(150,150,100,100);

  // fill(255,0,0,50);
  // rect(150,150,100,100);

  // push();
  // strokeWeight(3);
  // stroke('#5259E4');
  // fill('#BF71F4');
  // triangle(0, 0, 100, 100, 300, 0);
  // pop();

  // square(30, 20, 90,10,0,10,0);
  // rect(150,150,90,90);

  // quad(38, 31, 86, 20, 69, 63, 30, 76);

  // let num = 100;
  // push();
  // strokeWeight(3);
  // stroke('#5259E4');
  // fill('#BF71F4');
  // //arc parameter : CHORD(현), PIE or OPEN
  // arc(num    , 100, 100, 100, PI, PI*3/2, PIE);
  // pop();
  // arc(num+100, 100, 100, 100, 0 , PI);
  // arc(num+250, 100, 100, 100, 0 , PI*3/2);
  // arc(num+400, 100, 100, 100, 0 , PI*2);


  // strokeWeight(5);
  // noStroke();
  // ellipse(100, 100, 120, 120); 
  // stroke(0);
  // ellipse(200, 100, 120, 120);
  // stroke(255, 0, 0);
  // ellipse(300, 100, 120, 120);
  // stroke(0, 255, 0);
  // ellipse(400, 100, 120, 120);
  // stroke(0, 0, 255);
  // ellipse(500, 100, 120, 120);
  
  // noFill();
  // ellipse(100, 100, 120, 120); 
  // fill(0);
  // ellipse(200, 100, 120, 120);
  // fill(255, 0, 0);
  // ellipse(300, 100, 120, 120);
  // fill(0, 255, 0);
  // ellipse(400, 100, 120, 120);
  // fill(0, 0, 255);
  // ellipse(500, 100, 120, 120);

  noFill();
  ellipse(100, 100, 120, 120); 
  fill(0, 100);
  ellipse(200, 100, 120, 120);
  fill(255, 0, 0, 100);
  ellipse(300, 100, 120, 120);
  fill(0, 255, 0, 100); 
  ellipse(400, 100, 120, 120);
  fill(0, 0, 255, 100);
  ellipse(500, 100, 120, 120);

}

function draw(){
  
}
